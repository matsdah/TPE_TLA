/* Incompatibilidad de tipos: identificador de actor usado como variable numérica */
/* Sintácticamente válido (IDENTIFIER en expression) — falso positivo en Stage 2 */

character "Narrator" as narr color #FFFFFF;

set score = narr + 1;

scene "start" {
    narr "Using a character ID as a number.";
    end;
}
